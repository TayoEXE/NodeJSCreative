const randomGameName = require('./')
const test = require('tape')

test('assert a string type', function(t) {
  t.plan(1)

  t.equal(typeof randomGameName.random(), 'string', 'assert `randomGameName` type')

  t.end()
})
