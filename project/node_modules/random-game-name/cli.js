#!/usr/bin/env node
'use strict'
const meow = require('meow')
const randomGameName = require('./')

const cli = meow({
  help: [
    'Examples',
    '  $ random-game-name',
    '  Inept Caveman Overload'
  ]
})

console.log(randomGameName.random())
